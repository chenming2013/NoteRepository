兑现初始化过程：

1）. JVM将.class文件加载进内存
2）. 此时类被加载，类的静态(static)方法被执行,且只执行一次
3）. 对象创建，对象的属性被加载，并为属性赋默认初始化值（int型默认初始化值为0，String类型默认初始化值为null，Boolean型默认初始化值false）
4）. 为属性赋显示初始化值(如：name=“zhangsan”int=20)
5）. 执行构造代码块
6）. 执行构造函数